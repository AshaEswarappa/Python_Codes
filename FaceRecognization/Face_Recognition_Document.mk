The Face recognition files are built during the learning process in LinkedIn. Each files are put in different folder and output images are saved.

Face Detection : A picture is taken and used the built in fucntion of python to detect the faces in the picture.